% Author: Rasmus Pank Roulund
\documentclass[tikz, border=5pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{calc,trees,positioning,arrows,chains,shapes.geometric,%
    decorations.pathreplacing,decorations.pathmorphing,shapes,%
    matrix,shapes.symbols}

\tikzset{
>=stealth',
  punktchain/.style={
    rectangle,
    rounded corners,
    % fill=black!10,
    draw=black, very thick,
    text width=10em,
    minimum height=3em,
    text centered,
    on chain},
  line/.style={draw, thick, <-},
  element/.style={
    tape,
    top color=white,
    bottom color=blue!50!black!60!,
    minimum width=8em,
    draw=blue!40!black!90, very thick,
    text width=10em,
    minimum height=3.5em,
    text centered,
    on chain},
  every join/.style={->, thick,shorten >=1pt},
  decoration={brace},
  tuborg/.style={decorate},
  tubnode/.style={midway, right=2pt},
}
\begin{document}
\begin{tikzpicture}
	\node[very thick] at (-1.1,7) {\small Payments} ;
	\node[very thick] at (1,7) (inputs) {\small Inputs} ;
	\draw[very thick , -latex] (0.25, 8) + -- (0.25,6);
	\draw[very thick , -latex] (-0.25, 6) -- + (0,2);
	\node[draw , minimum height=2cm , minimum width=4cm , rounded corners] at (0,5) (system) {\small Health System} ;
	\node[draw , minimum height=2cm , minimum width=4cm  , rounded corners] at (0,1) (patients) {\small Patients} ;
	\node[very thick] at (1,3) (services){\small Services} ;
	\draw[very thick , -latex] (0.25, 4) + -- (0.25,2) ;

	\node[draw , minimum height=2cm , minimum width=4cm , rounded corners] at (5,3.5) (agency) {\small Purchasing Agency} ;

    \draw (agency) edge[out=180,in=0,->] (services);



\end{tikzpicture}
\end{document}
